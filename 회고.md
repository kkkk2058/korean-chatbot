# Korean Chatbot 프로젝트 회고

> AI 엔지니어 과제 — 한국어 언어모델을 바닥부터 단계적으로 구현
> 작성일: 2026-06-23

---

## 1. 과제 개요

- **목표**: 한국어 챗봇을 라이브러리에 의존하지 않고 바닥부터 만들어보며 LLM의 내부 원리를 체득
- **제약**: 개인 학습 프로젝트 / Colab(A100 40GB) 컴퓨팅 / 순수 Python 구현으로 인한 속도 한계
- **접근**: 3단계 점진적 구현
  - Stage 1 — 순수 Python BPE + 순수 PyTorch Transformer (직접 구현, 현재 서빙)
  - Stage 2 — HuggingFace `tokenizers` + `GPT2Config` 활용
  - Stage 3 — KoGPT2(`skt/kogpt2-base-v2`) 파인튜닝
- **결과물**: 위키 사전학습 → KoAlpaca 파인튜닝 → FastAPI 서빙 + 웹 UI까지 end-to-end 파이프라인
  - Stage 1 모델 약 52M 파라미터 (D_MODEL 512 / N_HEADS 8 / N_LAYERS 12), 가중치 ~210MB
  - 생성: temperature 0.8 / top_k 30 / repetition_penalty 1.3

---

## 2. 배운 것 (Learned)

**개념적 이해 — "해보기 전엔 몰랐던 것"**
- **BPE가 단순 빈도 병합이라는 것을 직접 구현하며 체감**: 자주 등장하는 문자 쌍을 반복 병합해 vocab을 키운다는 알고리즘을, 추상 개념이 아니라 코드로 이해함.
- **파인튜닝의 핵심은 loss 마스킹**: 단순히 데이터를 바꾸는 게 아니라, 질문 토큰은 `-100`으로 마스킹하고 **답변 토큰만 loss를 계산**해야 "답변하는 모델"로 바뀐다는 것. 이게 사전학습 모델과 챗봇을 가르는 지점이라는 걸 이해함.
- **causal mask / Pre-LayerNorm 구조**를 직접 짜며 Transformer 블록의 데이터 흐름(Attention → Add → FFN → Add)을 손에 익힘.

**엔지니어링 실전 교훈**
- **체크포인트는 모델 가중치만 저장하면 안 된다**: optimizer뿐 아니라 **scheduler/scaler state까지 저장**해야 이어학습 시 LR이 튀지 않는다. (아래 4번 실수 항목 참고)
- **FP16 mixed precision + gradient accumulation**으로 A100 40GB 안에서 실질 배치 64를 확보하는 법.
- **변환 결과 캐싱의 위력**: 데이터셋 토큰 변환 결과를 Drive에 캐시(`pretrain_ids.pt`)하니 런타임 재시작마다 반복되던 변환 시간이 사라짐.

---

## 3. 막힌 것 (Blocked / Struggled)

**(1) 순수 Python BPE 인코딩이 너무 느렸다 — 가장 오래 막힌 지점**
- 증상: 토크나이저 학습/데이터 변환이 약 **5시간** 단위로 걸려 실험 루프 자체가 불가능.
- 디버깅: 병목이 매 merge마다 전체 pair를 다시 세는 부분임을 확인.
- 해결: **① 증분 pair 카운트 업데이트 → ② pair 역인덱스 도입 → ③ 단어 단위 인코딩 영구 캐싱** 3단계로 개선해 **5시간 → 수 분**으로 단축.
  - (관련 커밋: `8274ace`, `4cff0b6`, `2a0d5f3`)

**(2) 이어학습 시 loss가 폭등했다**
- 증상: 체크포인트에서 재시작하면 loss가 정상 범위를 벗어나 튐.
- 가설/검증: 가중치는 복원되는데 LR 스케줄·scaler 상태가 초기화되어 학습률이 잘못 적용된다고 의심 → state 저장 누락 확인.
- 해결: 체크포인트에 scheduler/scaler state 추가 저장·복원 (`fbfbbdb`).

**(3) 소형 모델의 품질 한계 (끝까지 완전히 못 푼 것)**
- 약 52M 파라미터라 문장 단위 일관성은 되지만 **사실 정확성·긴 문맥은 약함**.
- 원인 가설: 순수 Python BPE 속도 한계 때문에 사전학습을 100k줄로 제한 → 사전학습 loss를 충분히 낮추지 못함.
- 미해결: 데이터를 늘려 검증해보지 못함 (시간/컴퓨팅 제약). → 6번 "다음에 한다면"으로 이월.

---

## 4. 실수 / 잘못된 판단 (Mistakes)

**(1) 체크포인트에 학습 상태를 일부만 저장한 것**
- 처음엔 모델 가중치(+optimizer)만 저장하면 이어학습이 되는 줄 알았음.
- 실제로는 scheduler/scaler state 누락으로 재시작마다 loss가 튀어, 원인을 찾는 데 시간을 소모.
- 교훈: "재현 가능한 학습"은 **랜덤성/스케줄/스케일러까지 포함한 전체 상태**의 문제라는 것을 체감.

**(2) 배치 사이즈를 메모리 확인 없이 크게 잡았다 (OOM)**
- BATCH_SIZE 64로 잡았다가 A100에서 OOM 발생 → 32로 조정 (`9f46fd4`).
- 이후 gradient accumulation으로 실질 배치를 다시 64로 맞추는 방식으로 정착. 처음부터 "메모리 배치 vs 실질 배치"를 분리해 생각했어야 함.

**(3) 토크나이저 학습 범위 설정을 오락가락했다**
- `max_corpus_lines` 제한을 뒀다가, 풀었다가(`ce93911`) 다시 100k로 제한(`3212ee8`)하는 등 결정이 흔들림.
- 원인: 속도와 품질의 트레이드오프를 미리 수치로 정해두지 않고 그때그때 대응함. 기준을 먼저 정했어야 함.

**(4) 구조를 여러 번 갈아엎었다**
- 폴더 구조를 `n_stage/` 도입 → stage별 분리 → `app/` 분리 등 여러 번 리팩터링.
- 초기에 단계별 구조를 설계하지 않고 시작해 중간에 비용을 치름. (다만 결과적으로 구조는 깔끔해짐)

---

## 5. 의사결정 & 트레이드오프

- **순수 Python 구현 고수 (속도 희생 ↔ 학습 효과)**: Stage 1에서 라이브러리를 안 쓰기로 한 건 느리지만 의도된 선택. 대신 Stage 2/3에서 라이브러리·pretrained로 실용성을 보완하는 단계 구성으로 균형을 맞춤.
- **사전학습 데이터 100k줄 제한 (완성도 ↔ 시간)**: 순수 Python BPE로 전체 코퍼스 변환은 비현실적이라 의도적으로 상한을 둠. 품질 한계는 알지만 "돌아가는 파이프라인 완성"을 우선.
- **Drive 캐시 도입 (저장공간 ↔ 반복 시간)**: 변환 결과·체크포인트를 Drive에 캐시해 Colab 런타임 휘발성 문제를 해결. 약간의 용량으로 반복 실행 비용을 크게 절감.
- **vocab/모델 크기 단계별 차등** (Stage1 16k·52M / Stage2 8k / Stage3 51k pretrained): 각 단계 목적(원리 이해 / 실용 / 성능)에 맞춰 의도적으로 다르게 설정.

---

## 6. 다음에 한다면 (Next time)

- **사전학습 데이터 확장 후 loss 검증**: 가장 먼저 시도할 것. `MAX_TRAIN_LINES`를 늘려 사전학습 loss를 더 낮췄을 때 생성 품질이 실제로 개선되는지 정량 확인.
- **정량 평가 도입**: 지금은 생성 결과를 눈으로 보는 수준. perplexity 외에 간단한 QA 정확도·응답 길이/반복률 등 자동 지표를 붙여 실험을 비교 가능하게 만들기.
- **구조를 먼저 설계하고 시작**: 단계별 폴더/config 분리를 처음부터 두면 중간 리팩터링 비용을 줄일 수 있음.
- **트레이드오프 기준을 사전에 수치로 고정**: "속도 X분 이내, 품질 Y 이상" 같은 기준을 먼저 정하고 데이터/배치 크기를 결정하기.
- **서빙 단계 보강**: 현재 단일 모델 서빙. 스트리밍 응답, 동시 요청 처리, 모델 교체(Stage 1/2/3) 스위치 등 운영 관점 보강 여지.

---

## 7. 엔지니어링 관점 회고 (Engineering mindset)

> 강의 코드를 참고해 위클리 챌린지를 "동작하게" 만드는 것과, **제약 안에서 견디는 코드**를 만드는 것 사이의 간극을 의식하게 됨. 이 프로젝트를 6개 렌즈로 다시 보며 정리.

### AI 엔지니어링은 웹/클라우드와 실패 양상이 다르다

| | 웹/클라우드 | AI/ML |
|---|---|---|
| 주 제약 | I/O·네트워크·동시성 | **GPU 메모리·시간·비용** |
| 에러 | 대개 터지고 재현됨 (4xx/5xx) | **조용히 망가짐** (NaN, 출력 붕괴, 품질 저하) |
| 한 번 실행 비용 | 밀리초 | **수 시간** → 함부로 재실행 불가 |
| 의존성 | 코드 + API | 코드 + **데이터**(데이터 버그가 모델 버그처럼 보임) |
| 상태 | 대체로 stateless | 학습 상태가 **중단을 견뎌야 함** |

→ 웹의 에러처리가 "try/except + 재시도 + 상태코드"라면, AI는 **"NaN 감지·grad norm 모니터링·체크포인트·데이터 검증·메모리 가드"**에 가깝다는 것을 체감.

### 신경 써야 할 6가지 원칙

1. **자원(메모리·시간·비용)을 1급 제약으로 둔다** — 루프를 짜기 전에 메모리 예산부터 계산. 메모리 배치와 실질 배치(grad accum) 분리.
2. **비싼 건 재개·캐시 가능하게 (resumability)** — 체크포인트는 model+optimizer+scheduler+scaler+RNG 전체 상태. 전처리 결과 캐싱.
3. **ML 에러는 조용하다 → 관측 가능성을 코드에 박는다** — loss·grad norm·lr·throughput 로깅, NaN 감지, validation loss.
4. **관심사를 "파일 종류"가 아니라 "생애주기"로 분리** — data → tokenizer → model → training → serving.
5. **데이터는 코드다** — 버전 관리·샘플 확인·분포 검증. "모델이 이상해요"의 절반은 데이터 버그.
6. **추론은 학습과 다른 시스템이다** — 비동기/스트리밍, 동적 배치(배치 내 최장 길이 패딩), `no_grad`, 동시성 제어.

### 내 코드를 이 기준으로 리뷰한 결과

**잘한 것**
- 체크포인트 shape에서 아키텍처 자동 추론 → 모델이 바뀌어도 서빙 코드 무수정.
- 추론에 `@torch.no_grad()` + `model.eval()` 정확히 적용, lifespan에서 모델 1회 로드.
- 학습/파인튜닝 loss 마스킹이 코드 전반에 일관됨.

**고쳐야 할 것 (우선순위順)**
- 🔴 **생성 루프가 O(n²)** — 매 토큰마다 전체 시퀀스를 재forward. **KV 캐시**로 마지막 토큰만 계산하면 O(n). (토크나이저에서 한 "이미 계산한 건 다시 안 한다"와 똑같은 원리인데 생성 루프에선 놓침)
- 🔴 **반복 페널티가 음수 logit에서 역작동** — `logit / penalty`는 음수일 때 확률을 오히려 올림. `logit>0 ? /penalty : *penalty`로. (조용한 품질 저하 버그의 실제 사례)
- 🔴 **서빙에 에러처리·입력검증·동시성 제어·스트리밍 부재** — 빈/초장문 입력 무방비, generate 예외 시 500 노출, 동시 요청 상한 없음.
- 🟡 **encode 캐시가 서빙에서 무한 증가** — 학습엔 이득이지만 장시간 서버에선 메모리 누수. LRU 상한 필요.
- 🟡 **학습 로직이 노트북에만 존재** (`src/dataset.py`·`train.py` 비어 있음) — 재사용·테스트·diff 리뷰 불가. 핵심 로직은 `src/` 함수로.
- 🟡 **`app/core/model.py`가 stage1 모델의 수동 사본** — 한쪽만 고치면 가중치 로드 깨짐(drift 위험).

### 직접 구현한 Stage 1 코드 리뷰 (모델·토크나이저)

> 가장 깊게 손댄 from-scratch 코드를 따로 점검. "버그"보다 **표준 기법 누락**이 핵심이라, 여기가 다음 학습 포인트가 가장 많다.

**Transformer (`stage1_from_scratch/src/model.py`)**
- 🔴 **weight tying 없음** — 입력 임베딩(`embedding`)과 출력 projection(`linear`)이 별도 가중치. 둘을 묶으면(`linear.weight = embedding.weight`) 약 16000×512 ≈ **8.2M 파라미터 절감**(52M 모델의 ~16%)에 보통 품질도 향상. 회고의 "소형 모델 한계" 개선과 직결되는 가장 임팩트 큰 수정.
- 🔴 **가중치 초기화 없음** — PyTorch 기본 init 사용 중(`nn.Embedding` 기본은 `N(0,1)`로 과도). GPT-2식 `normal(0, 0.02)` + residual projection을 `1/√(2·n_layers)`로 스케일하는 게 12층 깊이에서 수렴 안정성에 중요.
- 🟡 **패딩 마스크 없음** — causal mask만 적용. 배치 학습 시 실제 토큰이 pad를 attention 키로 참조함(loss는 -100으로 무시하지만 attention엔 섞임).
- 🟡 **causal mask를 매 forward마다 재생성** — `register_buffer`로 한 번 만들어 슬라이싱하는 게 효율적(생성 루프에서 토큰마다 반복됨).
- 🟢 잘한 것: `-1e4` 마스킹(FP16 NaN 회피), Pre-LN 구조, 임베딩 `√d_model` 스케일.

**Tokenizer — 내가 짠 첫 버전 vs 현재 버전 비교 (가장 배운 점)**

`tokenizer.py`의 **주석 처리된 윗부분이 내가 직접 짠 첫 구현**, **아래 활성 코드가 이후 AI 도움으로 개선한 버전**이다. 둘을 나란히 비교하니 내 사고의 약점이 정확히 보였다.

| 항목 | 내 첫 버전 (주석) | 현재 버전 (개선) |
|---|---|---|
| 인코딩 방식 | **greedy 최장일치** (매 위치마다 vocab 전체 스캔) | **학습된 merge를 순서대로 적용** (정통 BPE) |
| 학습 속도 | 매 merge마다 전체 corpus 순회 | **pair 역인덱스**로 해당 단어만 순회 |
| 공백 처리 | `SPACE_TOKEN` 치환 (SentencePiece식) | `</w>` 종료 마커 |

- **인코딩**: 내 방식은 "가장 긴 토큰부터 맞춰보기"였는데, 이건 BPE가 아니라 greedy longest-match라 **학습 때 분절과 추론 때 분절이 달라질 수 있다.** 정통 BPE는 학습된 merge 순서를 그대로 재현해야 일관성이 보장된다는 걸 비교하고 나서야 이해함.
- **속도**: 나도 증분 pair 카운팅까지는 떠올렸지만, 결정타였던 **"pair→단어 역인덱스"**를 떠올리지 못해 O(전체 단어)에 머물렀음. 역인덱스가 O(영향받는 단어)로 줄인 핵심. → "자료구조로 탐색 범위를 좁힌다"는 사고를 평소에 훈련해야 함.
- 🔴 **decode가 특수 토큰을 안 거름** (두 버전 공통) — `<pad>/<s>/</s>`가 문자열로 출력돼 서빙 쪽에서 수동으로 잘라내는 중. decode 자체에 `skip_special` 책임을 두는 게 맞음.


---

## 8. 강사님께 드리는 질문 (Questions)

> 엔지니어링 관점에서 코드를 어떻게 고쳐야 할지, 무엇부터 공부해야 할지 방향을 잡기 어려워 질문을 정리했습니다. 각 질문에 제가 가진 가설도 함께 적었으니, 방향이 맞는지 / 더 나은 길이 있는지 짚어주시면 감사하겠습니다.

### A. 구체적인 코드 수정에 대해

1. **생성 루프 최적화(KV 캐시)** — 현재 `engine.generate`는 토큰마다 전체 시퀀스를 다시 forward해 O(n²)입니다. 직접 구현한 Transformer에 KV 캐시를 붙이려면 attention의 forward 시그니처를 어떻게 바꾸는 게 정석인가요? 학습 코드까지 영향을 주지 않으려면 추론 경로만 분리하는 게 맞을까요?
2. **반복 페널티 버그** — `logit / penalty`가 음수 logit에서 역작동하는 걸 발견했습니다. `logit>0 ? /penalty : *penalty`로 고치려는데, 실무에서는 반복 억제를 penalty 방식과 `no_repeat_ngram` 중 무엇을 더 신뢰하나요? 둘을 같이 쓰는 게 일반적인가요?
3. **서빙 견고성** — `/chat` 엔드포인트에 입력 검증·에러처리·동시성 제어·스트리밍이 없습니다. 학습용 토이 프로젝트에서 이 중 **어디까지** 갖추는 게 적정선인가요? (과제 수준에서 오버엔지니어링과 최소 견고성의 경계가 궁금합니다)
4. **메모리 누수** — 토크나이저 `_encode_cache`가 서빙에서 무한 증가합니다. 학습엔 이득이라 같은 클래스를 공유 중인데, 학습용/추론용을 클래스로 분리하는 게 맞나요, 아니면 캐시 정책(LRU 등)만 주입하는 게 더 깔끔한가요?

### B. 구조·테스트에 대해

5. **노트북 vs 모듈** — 핵심 학습 로직이 `.ipynb`에만 있고 `src/dataset.py`·`train.py`는 비어 있습니다. 로직을 `src/` 함수로 빼고 노트북은 호출만 하게 바꾸려는데, Colab 기반 워크플로에서 이 분리를 실무자들은 실제로 어떻게 운용하나요? (노트북에서 모듈을 import할 때의 현실적인 불편함이 걱정됩니다)
6. **테스트** — ML 코드에서 단위 테스트는 어디까지 의미가 있나요? 토크나이저 encode/decode 왕복 같은 건 테스트가 명확한데, 학습 루프나 생성 품질은 무엇을 어떻게 검증하는 게 실무 관행인지 궁금합니다.
7. **모델 코드 중복** — `app/core/model.py`가 학습용 모델의 수동 사본입니다. 공통 패키지로 합치는 게 맞을까요, 아니면 서빙/학습 의존성을 분리하려고 일부러 사본을 두기도 하나요?

### C. 직접 구현한 Stage 1 모델·토크나이저에 대해

8. **weight tying** — 입력 임베딩과 출력 projection을 묶는 게 표준이라고 알게 됐는데, **왜** 가중치를 공유해도 (오히려) 품질이 좋아지는지 직관이 잘 안 잡힙니다. 항상 묶는 게 정답인가요, 안 묶는 게 나은 경우도 있나요?
9. **가중치 초기화** — 기본 init 대신 GPT-2식 `normal(0, 0.02)` + residual `1/√(2·n_layers)` 스케일을 써야 한다는데, 이 값들이 나온 근거와 init이 학습 안정성에 실제로 얼마나 영향을 주는지 궁금합니다. 제 12층 모델에서 init만 바꿔도 체감되는 차이가 날까요?
10. **인코딩 알고리즘** — 제 첫 BPE는 greedy 최장일치였고, 개선판은 학습된 merge를 순서대로 적용합니다. 이 둘이 실제로 결과가 갈리는 경우는 언제인가요? greedy 방식이 "틀렸다"고 봐야 하는 건지, 아니면 트레이드오프가 있는 선택지인지 짚어주시면 좋겠습니다.

11. **패딩 마스크** — causal mask만 있고 padding mask가 없는데, 우측 패딩 + loss -100 마스킹 조합에서 이게 실제 학습 품질에 유의미한 차이를 만드나요?

### D. 성장 방향에 대해

12. **무엇부터 공부할까** — 비동기·청킹·동적 배치·메모리 관리 등 모르는 게 많습니다. 지금 제 수준에서 **우선순위**를 어떻게 잡으면 좋을까요? (예: 추론 최적화 먼저 vs 데이터 파이프라인 먼저)

13. **자가 점검 기준** — 다음 프로젝트부터 코드를 스스로 리뷰할 때 쓸 체크리스트가 있다면 무엇을 넣어야 할까요? 제가 세운 "자원·실패·관측" 세 렌즈가 적절한 틀인지도 봐주시면 좋겠습니다.
