# 한국어 챗봇 — 프로젝트 상담 자료

> **한 줄 요약**: 라이브러리 없이 토크나이저·Transformer·학습 루프를 직접 구현해, 한국어 위키로 사전학습하고 KoAlpaca로 파인튜닝한 **약 52M from-scratch 한국어 LLM**. 토크나이저(자작 BPE ↔ 라이브러리)를 통제 비교하며 학습량 효과를 검증.

| | |
|---|---|
| 모델 | Decoder-only Transformer, ~52M (d_model 512 · 12층 · 8헤드 · seq 512) |
| 토크나이저 | vocab 16k — v1: 자작 순수 Python BPE / v2: HF ByteLevel BPE |
| 학습 | 사전학습(KoWiki) → 파인튜닝(KoAlpaca), 2단계 |
| 환경 | Google Colab A100, FP16 |
| 구현 범위 | 토크나이저·모델·학습·서빙(FastAPI+웹) **전부 직접** (v2 토크나이저만 라이브러리) |

---

## 1. 어떻게 학습시켰는지 (학습 데이터셋)

**2단계 파이프라인** — 언어를 먼저 배우고(사전학습), 답하는 형식을 배움(파인튜닝).

```
[토크나이저]  KoWiki → BPE(vocab 16k) 학습 → 고정(freeze)
[사전학습]    KoWiki 전체 → 블록 청킹(512토큰, 패딩 0%) → 15 epoch (lr 3e-4)
                    ↓ 가중치 이어받기
[파인튜닝]    KoAlpaca 21k → 답변 토큰만 학습(-100 마스킹) → 5 epoch (lr 1e-4, best-val 저장)
```

| 단계 | 데이터셋 | 방식 | 핵심 설정 |
|---|---|---|---|
| 토크나이저 | KoWiki (`wikimedia/wikipedia` 20231101.ko) | BPE, vocab 16k | v1 자작 / v2 HF ByteLevel |
| 사전학습 | KoWiki | next-token 예측, **블록 청킹** | 15 epoch, eff.batch 64, cosine LR |
| 파인튜닝 | `beomi/KoAlpaca-v1.1a` (21k Q&A) | `질문:\n답변:`, **답변만 loss** | 5 epoch, lr 1e-4, best-val |

**핵심 설계 포인트**
- **블록 청킹**: 줄 단위+패딩 대신 전체 토큰을 이어붙여 512씩 분할 → 패딩 0%, 문맥 100% 활용
- **답변-마스킹 파인튜닝**: 질문 토큰은 `-100`으로 가려 답변만 학습 → "답하는 모델"로 전환
- **토크나이저는 한 번 학습 후 고정** → 사전학습·파인튜닝·추론이 같은 토크나이저 재사용
- **데이터 → 모델 순서**: KoAlpaca(지시)는 사전학습용이 아니라 파인튜닝용. 지식은 사전학습(위키)에서.

---

## 2. 정성적 결과 (시연)

**학습량(사전학습 epoch)이 곧 품질** — 같은 모델인데 epoch만 늘려도 극적으로 개선됨.

| 사전학습 | val perplexity | "한국의 수도는?" 답변 |
|---|---|---|
| 3 epoch | ~134 | 호주·뉴질랜드 등 **완전 무관** (유창하지만 헛소리) |
| 15 epoch | ~40 | "**서울**을 수도로 정한 이유는…" (질문에 붙음) |

**15 epoch 모델(v2) 답변 예시**
```
👤 한국의 수도는 어디인가요?
🤖 한국은 '서울의 강남'이라는 뜻으로, 서울을 수도로 정한 이유는 …

👤 안녕?
🤖 안녕하세요! …

👤 핸드폰 추천해줘
🤖 핸드폰 추천해봐요? 1. …   (추천=목록 형식 인식)
```

→ **"형식만 배운 단계(헛소리) → 질문 유형 인식 + 관련 답변 시도"**로 도약.
문법·주제 인식·답변 형식은 습득. (사실 정확성은 아래 한계점)

---

## 3. 현재 한계점

| 한계 | 원인 |
|---|---|
| **사실 정확도 낮음** | 위키는 QA 지식 형태가 아님 + 약 52M 소형 모델 |
| **데이터 부족 (data-starved)** | 50M 모델이 요구하는 토큰 수 대비 데이터 부족 → 지식 용량 한계 |
| **단일턴만 가능** | KoAlpaca가 단일턴 Q&A |
| **긴 답변 드리프트·꼬리 깨짐** | 소형 모델 + 확률 샘플링; 길어질수록 주제 이탈 |

**해결한 것**: 토크나이저 글자 깨짐/`<unk>` 없음, 공백·줄바꿈 보존(v2), 학습 파이프라인 안정성, 반복 루프(no-repeat-ngram).

---

## 4. 앞으로의 방향성

1. **데이터 확대·다양화** (가장 큰 레버) — 위키 외 뉴스·교과서 등 추가
2. **측정하며 스케일업** — val ppl(또는 bpc) 추적하며 모델 크기↑ *(데이터가 받쳐줄 때만)*
3. **멀티턴 + 도구 호출** instruction 튜닝
4. **구조 개선** — weight tying(임베딩-출력층 공유), LR warmup

> 원칙: **데이터 먼저, 모델은 따라온다.** 데이터를 키우기 전에 모델만 키우면 overfit만 심해짐.

---

## 5. 비교 평가 시스템

### 5-1. 핵심 발견 — perplexity는 토크나이저가 다르면 비교 불가

같은 데이터·모델·파이프라인에서 **토크나이저만** 바꿔 통제 비교:

| | v1 (자작 BPE, 음절 위주) | v2 (HF ByteLevel, subword) |
|---|---|---|
| 사전학습 val ppl | **~32 (더 낮음)** | ~40 |
| 파인튜닝 val ppl | **~24 (더 낮음)** | ~32 |
| "안녕?" 인사 인식 | ❌ (2회 연속 실패) | ✅ |
| "핸드폰 추천" 요청 인식 | ❌ | ✅ |
| 질문에 붙는 답변 | 약함 | 상대적으로 강함 |

**v1이 perplexity는 더 낮은데 생성 품질은 더 나쁨.** 이유:
- v1은 거의 음절 단위 → 단어 내부 음절 예측이 거의 결정적 → loss가 **거저** 낮아짐
- 평균 loss를 쉬운 음절 토큰이 지배 → 정작 내용 토큰은 묻혀 얕은 통계만 학습
- 토큰 수가 많아 "질문 의도"가 더 긴 시퀀스를 견뎌야 함 → 생성 중 드리프트

→ **교훈: 토크나이저가 다르면 ppl로 비교하면 안 됨.** 글자당 손실(bits-per-char) 또는 정성 평가로 판단. 좋은 subword 토크나이저(v2)는 토큰 효율이 높아 실제 응답 품질을 끌어올림.

### 5-2. 평가 데이터셋 (질문 + 정답, 12문항)

| # | 카테고리 | 질문 | 정답(핵심) |
|---|---|---|---|
| 1 | 사실 | 한국의 수도는 어디인가요? | 서울 |
| 2 | 사실 | 물은 어떤 원소로 이루어져 있나요? | 수소와 산소 (H₂O) |
| 3 | 사실 | 태양계에서 가장 큰 행성은 무엇인가요? | 목성 |
| 4 | 사실 | 한글을 만든 사람은 누구인가요? | 세종대왕 |
| 5 | 사실 | 1년은 며칠인가요? | 365일 (윤년 366일) |
| 6 | 정의 | 광합성이란 무엇인가요? | 식물이 빛으로 CO₂·물에서 양분과 산소를 만드는 과정 |
| 7 | 정의 | 중력이란 무엇인가요? | 질량이 있는 물체끼리 끌어당기는 힘 |
| 8 | 추론 | 얼음이 녹으면 무엇이 되나요? | 물 |
| 9 | 추론 | 비가 올 때 무엇을 챙기면 좋을까요? | 우산 |
| 10 | 계산 | 2 더하기 3은 얼마인가요? | 5 |
| 11 | 대화 | 안녕하세요? | 인사 응대 |
| 12 | 대화 | 고마워 | 천만에요/별말씀을 류 |

> 카테고리 의도: **사실** = 데이터 한계 드러냄 · **정의/추론** = 일반화 · **대화** = 파인튜닝 효과. 카테고리별 점수를 따로 내면 약점이 보임.

### 5-3. 채점 기준 (rubric)

| 정확도 | 점수 | | 유창성 | 점수 |
|---|---|---|---|---|
| 정답·핵심 포함 | 2 | | 자연스러운 한국어 | 2 |
| 부분/모호 | 1 | | 비문 일부 | 1 |
| 오답·헛소리 | 0 | | 깨짐/반복 | 0 |

> 항목당 정확도(0~2)+유창성(0~2), 12문항 합산. **생성은 확률 샘플링이라 문항당 2~3회 돌려 집계.** 토크나이저 비교 시 ppl 대신 이 점수(또는 bpc)를 사용.

### 5-4. 비교 축

| 축 | 무엇을 보나 |
|---|---|
| v1(자작) ↔ v2(라이브러리) | 토크나이저 효과 |
| 사전학습 3 ↔ 15 epoch | 학습량 효과 (가장 임팩트 큼) |
| 자동: 정답에 대한 bits-per-char | 사람 채점 없이 토크나이저-독립 비교 |

---

## 상담 포인트 (조언 구하고 싶은 것)

1. **스케일 우선순위** — 지금 50M·데이터 부족 상태에서 *데이터 확대*와 *모델 축소* 중 무엇이 먼저? (Chinchilla 관점)
2. **토크나이저 비교 방법론** — "ppl 함정 → bpc/정성 평가" 결론이 타당한가? 더 엄밀한 지표가 있나?
3. **사실 정확도 개선** — 위키+KoAlpaca 범위에서 사실성을 끌어올릴 현실적 방법? (RAG? 데이터 큐레이션?)
4. **소형 모델 데모 운용** — 확률 샘플링이라 답이 매번 다른데, 시연/평가에서 "여러 번 중 최선" 방식이 정당한가?
5. **평가셋 설계** — 12문항 + 0~2 rubric 구성이 적절한가? 자동화(LLM-judge 등) 도입할 가치?

---

### 부록 — 핵심 하이퍼파라미터

| 구분 | 값 |
|---|---|
| 모델 | d_model 512 · n_heads 8 · n_layers 12 · max_seq_len 512 · dropout 0.1 |
| 사전학습 | 15 epoch · lr 3e-4(cosine) · batch 16×4(=64) · FP16 |
| 파인튜닝 | 5 epoch · lr 1e-4 · batch 16×4(=64) · best-val 저장 |
| 생성 | temperature 0.8 · top_k 30 · repetition_penalty 1.3 · max_new_tokens 100 |
